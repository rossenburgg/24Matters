module.exports = {
  testEnvironment: 'node', // Specify the test environment is node
  clearMocks: true, // Automatically clear mock calls, instances and results before every test
  coverageDirectory: 'coverage', // Specify where Jest should output its coverage files
  coverageProvider: 'v8', // Use V8's coverage tool
};